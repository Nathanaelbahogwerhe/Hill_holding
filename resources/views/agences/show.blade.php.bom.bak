@extends('layouts.app')

@section('title', 'D̩tails de l\'agence')

@section('content')
<div class="max-w-xl mx-auto bg-hh-card p-6 rounded shadow">
    <h2 class="text-lg font-semibold mb-4">D̩tails de l'agence</h2>

    <div class="mb-4">
        <strong>Nom :</strong> {{ $agence->name }}
    </div>
    <div class="mb-4">
        <strong>Filiale :</strong> {{ $agence->filiale->name ?? '-' }}
    </div>
    <div class="mb-4">
        <strong>Adresse :</strong> {{ $agence->address ?? '-' }}
    </div>
    <div class="mb-4">
        <strong>T̩l̩phone :</strong> {{ $agence->phone ?? '-' }}
    </div>

    <a href="{{ route('agences.edit', $agence) }}" class="btn btn-primary">Modifier</a>
    <a href="{{ route('agences.index') }}" class="btn btn-secondary">Retour</a>
</div>
@endsection







