@extends('layouts.app')

@section('content')
    <h1>Liste des permissions</h1>
    <a href="{{ route('permissions.create') }}">+ Nouvelle permission</a>
    <ul>
        @foreach ($permissions as $permission)
            <li>
                <strong>{{ $permission->name }}</strong>
                <a href="{{ route('permissions.edit', $permission->id) }}">âœï¸</a>
                <form action="{{ route('permissions.destroy', $permission->id) }}" method="POST" style="display:inline">
                    @csrf
                    @method('DELETE')
                    <button type="submit">ðŸ—‘ï¸</button>
                </form>
            </li>
        @endforeach
    </ul>
@endsection







