@extends('layouts.app')

@section('content')
    <h1>Liste des rÃ´les</h1>
    <a href="{{ route('roles.create') }}">+ Nouveau rÃ´le</a>
    <ul>
        @foreach ($roles as $role)
            <li>
                <strong>{{ $role->name }}</strong> 
                ({{ $role->permissions->pluck('name')->join(', ') }})
                <a href="{{ route('roles.edit', $role->id) }}">âœï¸</a>
                <form action="{{ route('roles.destroy', $role->id) }}" method="POST" style="display:inline">
                    @csrf
                    @method('DELETE')
                    <button type="submit">ðŸ—‘ï¸</button>
                </form>
            </li>
        @endforeach
    </ul>
@endsection
@section('styles')
    <style>
        ul {
            list-style-type: none;
            padding: 0;
        }
        li {
            margin-bottom: 10px;
        }
        a {
            margin-left: 10px;
            text-decoration: none;
        }
        button {
            background: none;
            border: none;
            color: red;
            cursor: pointer;
        }
    </style>







