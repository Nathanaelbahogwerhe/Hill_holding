@extends('layouts.app')

@section('title', 'Ventes')

@section('content')
<div class="bg-white dark:bg-hh-gray-dark shadow-md rounded-lg p-6">
    <div class="flex justify-between items-center mb-4">
        <h1 class="text-2xl font-semibold">Liste des ventes</h1>
        <a href="{{ route('sales.create') }}" 
           class="bg-hh-blue hover:bg-hh-blue-dark text-white px-4 py-2 rounded">
           + Nouvelle vente
        </a>
    </div>

    <table class="min-w-full border border-gray-200 dark:border-hh-gray-darker text-left">
        <thead class="bg-hh-gray-light dark:bg-hh-gray-darker">
            <tr>
                <th class="p-2">#</th>
                <th class="p-2">Client</th>
                <th class="p-2">Produit/Service</th>
                <th class="p-2">Quantit̩</th>
                <th class="p-2">Montant total</th>
                <th class="p-2">Date</th>
                <th class="p-2 text-right">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach ($sales as $sale)
                <tr class="border-t dark:border-hh-gray-dark hover:bg-hh-gray-light/50 dark:hover:bg-hh-gray-darker/50">
                    <td class="p-2">{{ $loop->iteration }}</td>
                    <td class="p-2">{{ $sale->client->name ?? '-' }}</td>
                    <td class="p-2">{{ $sale->product->name ?? $sale->service->name ?? '-' }}</td>
                    <td class="p-2">{{ $sale->quantity }}</td>
                    <td class="p-2">{{ number_format($sale->total_amount, 2) }} {{ config('app.currency', 'USD') }}</td>
                    <td class="p-2">{{ \Carbon\Carbon::parse($sale->date)->format('d/m/Y') }}</td>
                    <td class="p-2 text-right space-x-2">
                        <a href="{{ route('sales.show', $sale) }}" class="text-hh-blue">Voir</a>
                        <a href="{{ route('sales.edit', $sale) }}" class="text-hh-green">Modifier</a>
                        <form action="{{ route('sales.destroy', $sale) }}" method="POST" class="inline">
                            @csrf @method('DELETE')
                            <button type="submit" class="text-red-500" onclick="return confirm('Supprimer cette vente ?')">
                                Supprimer
                            </button>
                        </form>
                    </td>
                </tr>
            @endforeach
        </tbody>
    </table>
</div>
@endsection







