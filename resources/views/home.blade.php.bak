@extends('layouts.app')

@section('title', 'Tableau de bord - Hill Holding')

@section('content')
<div class="space-y-8">

    <!-- Bandeau supérieur -->
    <div class="bg-gradient-to-r from-yellow-500 to-yellow-600 text-black rounded-2xl shadow-lg p-8 flex justify-between items-center">
        <div>
            <h1 class="text-3xl font-extrabold">Bienvenue, {{ auth()->user()->name }} 👋</h1>
            <p class="mt-2 text-lg">Voici le tableau de bord global de <strong>Hill Holding</strong>.</p>
        </div>
        <div class="text-7xl opacity-20">
            <x-heroicon-o-building-office class="w-20 h-20"/>
        </div>
    </div>

    <!-- Action rapide: Ajouter un employé -->
    <div class="flex justify-end -mt-6 mr-2">
        <a href="{{ route('employees.create') }}"
           class="inline-flex items-center gap-2 px-4 py-2 bg-yellow-500 text-black rounded-lg shadow-lg hover:bg-yellow-600 transition">
            <!-- SVG inline user-add (fiable même sans composant) -->
            <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M16 21v-2a4 4 0 00-4-4H6a4 4 0 00-4 4v2"/>
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 11a4 4 0 100-8 4 4 0 000 8z"/>
                <path stroke-linecap="round" stroke-linejoin="round" d="M20 8v6M23 11h-6"/>
            </svg>
            Ajouter un employé
        </a>
    </div>

    <!-- Résumé statistique global -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <x-dashboard-card title="Employés" :count="$employeeCount ?? 0" icon="users" route="employees.index"/>
        <x-dashboard-card title="Projets" :count="$projectCount ?? 0" icon="clipboard-list" route="projects.index"/>
        <x-dashboard-card title="Revenus" :count="$salesCount ?? 0" icon="currency-dollar" route="revenues.index"/>
    </div>

    <!-- Modules centraux -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mt-10">

        <!-- Module RH -->
        <div class="bg-black text-yellow-500 rounded-2xl shadow-lg p-6">
            <h2 class="text-2xl font-semibold mb-4 border-b border-yellow-600 pb-2">Ressources Humaines</h2>
            <ul class="space-y-3">
                <li><a href="{{ route('employees.index') }}" class="hover:text-yellow-300 flex items-center"><x-heroicon-o-user class="w-5 h-5 mr-2"/> Gestion du personnel</a></li>
                <li><a href="{{ route('departments.index') }}" class="hover:text-yellow-300 flex items-center"><x-heroicon-o-briefcase class="w-5 h-5 mr-2"/> Départements</a></li>
                <li><a href="{{ route('attendances.index') }}" class="hover:text-yellow-300 flex items-center"><x-heroicon-o-calendar class="w-5 h-5 mr-2"/> Présences</a></li>
                <li><a href="{{ route('payrolls.index') }}" class="hover:text-yellow-300 flex items-center"><x-heroicon-o-banknotes class="w-5 h-5 mr-2"/> Paies et salaires</a></li>
            </ul>
        </div>

        <!-- Module Opérations -->
        <div class="bg-black text-yellow-500 rounded-2xl shadow-lg p-6">
            <h2 class="text-2xl font-semibold mb-4 border-b border-yellow-600 pb-2">Opérations</h2>
            <ul class="space-y-3">
                <li><a href="{{ route('projects.index') }}" class="hover:text-yellow-300 flex items-center"><x-heroicon-o-clipboard-document class="w-5 h-5 mr-2"/> Suivi des projets</a></li>
                <li><a href="{{ route('clients.index') }}" class="hover:text-yellow-300 flex items-center"><x-heroicon-o-user-group class="w-5 h-5 mr-2"/> Gestion clients</a></li>
                <li><a href="{{ route('products.index') }}" class="hover:text-yellow-300 flex items-center"><x-heroicon-o-truck class="w-5 h-5 mr-2"/> Produits</a></li>
                <li><a href="{{ route('stock_transfers.index') }}" class="hover:text-yellow-300 flex items-center"><x-heroicon-o-archive-box class="w-5 h-5 mr-2"/> Transferts de stock</a></li>
            </ul>
        </div>

        <!-- Module Finance -->
        <div class="bg-black text-yellow-500 rounded-2xl shadow-lg p-6">
            <h2 class="text-2xl font-semibold mb-4 border-b border-yellow-600 pb-2">Finance</h2>
            <ul class="space-y-3">
                <li><a href="{{ route('revenues.index') }}" class="hover:text-yellow-300 flex items-center"><x-heroicon-o-chart-bar class="w-5 h-5 mr-2"/> Revenus</a></li>
                <li><a href="{{ route('expenses.index') }}" class="hover:text-yellow-300 flex items-center"><x-heroicon-o-credit-card class="w-5 h-5 mr-2"/> Dépenses</a></li>
                <li><a href="{{ route('finances.index') }}" class="hover:text-yellow-300 flex items-center"><x-heroicon-o-banknotes class="w-5 h-5 mr-2"/> Finances</a></li>
                <li><a href="{{ route('financial_reports.index') }}" class="hover:text-yellow-300 flex items-center"><x-heroicon-o-document-chart-bar class="w-5 h-5 mr-2"/> Rapports financiers</a></li>
            </ul>
        </div>

    </div>

</div>
@endsection