@extends('layouts.app')

@php
    $user = auth()->user();
    $niveau = 'mere';
    $filiales = \App\Models\Filiale::with('agences')->get();
    $users = \App\Models\User::all();
@endphp

@section('title', 'Dashboard Maison MÃ¨re')

@section('content')
<h2 class="mb-4">ðŸ¢ Maison MÃ¨re</h2>

<div class="row">
    <div class="col-md-6">
        <h4>Filiales et Agences</h4>
        <ul>
            @foreach($filiales as $f)
                <li>{{ $f->name }} ({{ $f->code }})
                    <ul>
                        @foreach($f->agences as $a)
                            <li>{{ $a->name }} ({{ $a->code }})</li>
                        @endforeach
                    </ul>
                </li>
            @endforeach
        </ul>
    </div>

    <div class="col-md-6">
        <h4>Utilisateurs</h4>
        <ul>
            @foreach($users as $u)
                <li>{{ $u->name }} - {{ $u->email }}</li>
            @endforeach
        </ul>
    </div>
</div>
@endsection







