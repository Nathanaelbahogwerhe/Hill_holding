@extends('layouts.app')

@section('title', 'D̩tails Produit')

@section('content')
<div class="bg-white dark:bg-hh-gray-dark shadow-md rounded-lg p-6">
    <h1 class="text-2xl font-semibold mb-4">D̩tails du produit</h1>

    <div class="space-y-2">
        <p><strong>Nom :</strong> {{ $product->name }}</p>
        <p><strong>Cat̩gorie :</strong> {{ $product->category ?? '-' }}</p>
        <p><strong>Fournisseur :</strong> {{ $product->supplier?->name ?? '-' }}</p>
        <p><strong>Prix :</strong> {{ number_format($product->price, 2) }} {{ config('app.currency', 'USD') }}</p>
        <p><strong>Stock :</strong> {{ $product->stock }}</p>
    </div>

    <div class="flex justify-end mt-4 space-x-2">
        <a href="{{ route('products.edit', $product) }}" class="bg-hh-green hover:bg-hh-green-dark text-white px-4 py-2 rounded">Modifier</a>
        <a href="{{ route('products.index') }}" class="bg-hh-gray hover:bg-hh-gray-dark text-white px-4 py-2 rounded">Retour</a>
    </div>
</div>
@endsection







