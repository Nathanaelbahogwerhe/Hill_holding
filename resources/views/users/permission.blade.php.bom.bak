@extends('layouts.app')

@section('content')
<div class="p-6">
    <h1 class="text-2xl font-bold mb-4">Permissions de {{ $user->name }}</h1>

    <form method="POST" action="{{ route('users.permissions.update', $user) }}">
        @csrf

        <div class="grid grid-cols-2 gap-4 mb-4">
            @foreach($permissions as $permission)
                <label class="flex items-center space-x-2">
                    <input type="checkbox" name="permissions[]" value="{{ $permission->name }}"
                        {{ $user->hasPermissionTo($permission->name) ? 'checked' : '' }}>
                    <span>{{ $permission->name }}</span>
                </label>
            @endforeach
        </div>

        <button type="submit" class="px-4 py-2 bg-green-600 text-white rounded">Enregistrer âœ…</button>
        <a href="{{ route('users.index') }}" class="px-4 py-2 bg-gray-500 text-white rounded ml-2">Annuler</a>
    </form>
</div>
@endsection







