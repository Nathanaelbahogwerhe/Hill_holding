@extends('layouts.app')

@section('content')
<div class="container mx-auto p-6">
    <h1 class="text-2xl font-bold mb-6 text-yellow-500">âž• Nouveau message</h1>

    <form action="{{ route('messages.store') }}" method="POST" enctype="multipart/form-data" class="bg-gray-900 text-white p-6 rounded-xl shadow">
        @csrf

        <div class="mb-4">
            <label class="block mb-2">Destinataire</label>
            <select name="recipient_id" class="w-full bg-gray-800 rounded p-2" required>
                <option value="">-- SÃ©lectionner --</option>
                @foreach($recipients as $recipient)
                    <option value="{{ $recipient->id }}">{{ $recipient->first_name }} {{ $recipient->last_name }}</option>
                @endforeach
            </select>
            @error('recipient_id') <p class="text-red-500 text-sm">{{ $message }}</p> @enderror
        </div>

        <div class="mb-4">
            <label class="block mb-2">Objet</label>
            <input type="text" name="subject" class="w-full bg-gray-800 rounded p-2" value="{{ old('subject') }}" required>
            @error('subject') <p class="text-red-500 text-sm">{{ $message }}</p> @enderror
        </div>

        <div class="mb-4">
            <label class="block mb-2">Message</label>
            <textarea name="body" class="w-full bg-gray-800 rounded p-2 h-32" required>{{ old('body') }}</textarea>
            @error('body') <p class="text-red-500 text-sm">{{ $message }}</p> @enderror
        </div>

        <div class="mb-6">
            <label class="block mb-2">ðŸ“Ž PiÃ¨ce jointe (optionnel)</label>
            <input type="file" name="attachment" class="w-full text-gray-300">
            @error('attachment') <p class="text-red-500 text-sm">{{ $message }}</p> @enderror
        </div>

        <div class="flex justify-end">
            <button type="submit" class="bg-yellow-500 text-black px-4 py-2 rounded hover:bg-yellow-600">
                Envoyer âœ‰ï¸
            </button>
        </div>
    </form>
</div>
@endsection







