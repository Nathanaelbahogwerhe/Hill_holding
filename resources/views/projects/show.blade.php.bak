@extends('layouts.app')

@section('title', 'Détails du projet')

@section('content')
<div class="max-w-4xl mx-auto bg-white dark:bg-hh-gray-dark rounded-lg shadow p-6 space-y-6">
    <div class="flex justify-between items-center">
        <h2 class="text-2xl font-semibold flex items-center space-x-2">
            <x-heroicon-o-briefcase class="w-6 h-6 text-hh-primary"/>
            <span>{{ $project->name }}</span>
        </h2>
        <div class="space-x-2">
            <a href="{{ route('projects.edit', $project) }}" 
               class="px-3 py-2 bg-yellow-400 text-white rounded hover:bg-yellow-500">
               <x-heroicon-o-pencil-square class="w-5 h-5 inline"/> Modifier
            </a>
            <a href="{{ route('projects.index') }}" 
               class="px-3 py-2 bg-gray-200 rounded hover:bg-gray-300">Retour</a>
        </div>
    </div>

    <div class="grid grid-cols-2 gap-4">
        <div>
            <p class="text-sm text-gray-500">Responsable</p>
            <p class="font-medium">{{ $project->manager->name ?? 'Non assigné' }}</p>
        </div>
        <div>
            <p class="text-sm text-gray-500">Statut</p>
            <p>
                <span class="px-2 py-1 rounded text-xs font-medium 
                    {{ $project->status === 'En cours' ? 'bg-yellow-100 text-yellow-700' : 
                       ($project->status === 'Terminé' ? 'bg-green-100 text-green-700' : 
                       'bg-gray-100 text-gray-700') }}">
                    {{ $project->status }}
                </span>
            </p>
        </div>

        <div>
            <p class="text-sm text-gray-500">Date de début</p>
            <p class="font-medium">{{ $project->start_date?->format('d/m/Y') ?? '—' }}</p>
        </div>
        <div>
            <p class="text-sm text-gray-500">Date de fin</p>
            <p class="font-medium">{{ $project->end_date?->format('d/m/Y') ?? '—' }}</p>
        </div>
    </div>

    <div>
        <p class="text-sm text-gray-500 mb-1">Description</p>
        <div class="p-4 bg-hh-gray-light dark:bg-hh-gray-darker rounded-lg">
            {{ $project->description ?? 'Aucune description.' }}
        </div>
    </div>
</div>
@endsection
