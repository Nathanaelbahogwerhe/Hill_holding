@extends('layouts.app')

@section('title', 'Créer un Projet')

@section('content')
<div class="max-w-3xl mx-auto bg-white dark:bg-hh-gray-dark rounded-lg shadow p-6">
    <h2 class="text-xl font-semibold mb-4 flex items-center space-x-2">
        <x-heroicon-o-plus class="w-5 h-5 text-hh-primary"/>
        <span>Nouveau Projet</span>
    </h2>

    <form method="POST" action="{{ route('projects.store') }}" class="space-y-4">
        @csrf

        <div>
            <label class="block text-sm font-medium mb-1">Nom du projet</label>
            <input type="text" name="name" value="{{ old('name') }}" 
                   class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark" required>
        </div>

        <div>
            <label class="block text-sm font-medium mb-1">Responsable</label>
            <select name="manager_id" class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark">
                <option value="">-- Sélectionner --</option>
                @foreach($managers as $manager)
                    <option value="{{ $manager->id }}">{{ $manager->name }}</option>
                @endforeach
            </select>
        </div>

        <div class="grid grid-cols-2 gap-4">
            <div>
                <label class="block text-sm font-medium mb-1">Date de début</label>
                <input type="date" name="start_date" class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark">
            </div>
            <div>
                <label class="block text-sm font-medium mb-1">Date de fin</label>
                <input type="date" name="end_date" class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark">
            </div>
        </div>

        <div>
            <label class="block text-sm font-medium mb-1">Statut</label>
            <select name="status" class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark">
                <option value="Planifié">Planifié</option>
                <option value="En cours">En cours</option>
                <option value="Terminé">Terminé</option>
            </select>
        </div>

        <div>
            <label class="block text-sm font-medium mb-1">Description</label>
            <textarea name="description" rows="4" 
                      class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark"></textarea>
        </div>

        <div class="flex justify-end">
            <a href="{{ route('projects.index') }}" class="px-4 py-2 bg-gray-200 rounded hover:bg-gray-300 mr-2">Annuler</a>
            <button type="submit" class="px-4 py-2 bg-hh-primary text-white rounded hover:bg-hh-primary-dark">Enregistrer</button>
        </div>
    </form>
</div>
@endsection
