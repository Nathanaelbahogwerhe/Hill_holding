@extends('layouts.app')

@section('title', 'Modifier le projet')

@section('content')
<div class="max-w-3xl mx-auto bg-white dark:bg-hh-gray-dark rounded-lg shadow p-6">
    <h2 class="text-xl font-semibold mb-4 flex items-center space-x-2">
        <x-heroicon-o-pencil-square class="w-5 h-5 text-hh-primary"/>
        <span>Modifier le projet</span>
    </h2>

    <form method="POST" action="{{ route('projects.update', $project) }}" class="space-y-4">
        @csrf
        @method('PUT')

        <div>
            <label class="block text-sm font-medium mb-1">Nom du projet</label>
            <input type="text" name="name" value="{{ old('name', $project->name) }}" 
                   class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark" required>
        </div>

        <div>
            <label class="block text-sm font-medium mb-1">Responsable</label>
            <select name="manager_id" class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark">
                <option value="">-- Sélectionner --</option>
                @foreach($managers as $manager)
                    <option value="{{ $manager->id }}" @selected($manager->id == $project->manager_id)>
                        {{ $manager->name }}
                    </option>
                @endforeach
            </select>
        </div>

        <div class="grid grid-cols-2 gap-4">
            <div>
                <label class="block text-sm font-medium mb-1">Date de début</label>
                <input type="date" name="start_date" 
                       value="{{ old('start_date', $project->start_date?->format('Y-m-d')) }}" 
                       class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark">
            </div>
            <div>
                <label class="block text-sm font-medium mb-1">Date de fin</label>
                <input type="date" name="end_date" 
                       value="{{ old('end_date', $project->end_date?->format('Y-m-d')) }}" 
                       class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark">
            </div>
        </div>

        <div>
            <label class="block text-sm font-medium mb-1">Statut</label>
            <select name="status" class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark">
                <option value="Planifié" @selected($project->status === 'Planifié')>Planifié</option>
                <option value="En cours" @selected($project->status === 'En cours')>En cours</option>
                <option value="Terminé" @selected($project->status === 'Terminé')>Terminé</option>
            </select>
        </div>

        <div>
            <label class="block text-sm font-medium mb-1">Description</label>
            <textarea name="description" rows="4" 
                      class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark">{{ old('description', $project->description) }}</textarea>
        </div>

        <div class="flex justify-end">
            <a href="{{ route('projects.index') }}" class="px-4 py-2 bg-gray-200 rounded hover:bg-gray-300 mr-2">Annuler</a>
            <button type="submit" class="px-4 py-2 bg-hh-primary text-white rounded hover:bg-hh-primary-dark">Mettre à jour</button>
        </div>
    </form>
</div>
@endsection
