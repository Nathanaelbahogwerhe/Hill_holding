@extends('layouts.app')

@section('title', 'Nouvelle tâche')

@section('content')
<div class="max-w-3xl mx-auto bg-white dark:bg-hh-gray-dark rounded-lg shadow p-6">
    <h2 class="text-xl font-semibold mb-4 flex items-center space-x-2">
        <x-heroicon-o-clipboard-document-check class="w-5 h-5 text-hh-primary"/>
        <span>Créer une nouvelle tâche</span>
    </h2>

    <form method="POST" action="{{ route('tasks.store') }}" class="space-y-4">
        @csrf

        <div>
            <label class="block text-sm font-medium mb-1">Titre</label>
            <input type="text" name="title" value="{{ old('title') }}" 
                   class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark" required>
        </div>

        <div>
            <label class="block text-sm font-medium mb-1">Projet associé</label>
            <select name="project_id" class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark" required>
                <option value="">-- Sélectionner un projet --</option>
                @foreach($projects as $project)
                    <option value="{{ $project->id }}">{{ $project->name }}</option>
                @endforeach
            </select>
        </div>

        <div>
            <label class="block text-sm font-medium mb-1">Assigné à</label>
            <select name="assigned_to" class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark">
                <option value="">-- Non assigné --</option>
                @foreach($employees as $employee)
                    <option value="{{ $employee->id }}">{{ $employee->name }}</option>
                @endforeach
            </select>
        </div>

        <div class="grid grid-cols-2 gap-4">
            <div>
                <label class="block text-sm font-medium mb-1">Date de début</label>
                <input type="date" name="start_date" value="{{ old('start_date') }}"
                       class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark">
            </div>
            <div>
                <label class="block text-sm font-medium mb-1">Date d’échéance</label>
                <input type="date" name="due_date" value="{{ old('due_date') }}"
                       class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark">
            </div>
        </div>

        <div>
            <label class="block text-sm font-medium mb-1">Statut</label>
            <select name="status" class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark">
                <option value="Planifiée">Planifiée</option>
                <option value="En cours">En cours</option>
                <option value="Terminée">Terminée</option>
            </select>
        </div>

        <div>
            <label class="block text-sm font-medium mb-1">Description</label>
            <textarea name="description" rows="4" 
                      class="w-full rounded border-gray-300 dark:bg-hh-gray-darker dark:border-hh-gray-dark">{{ old('description') }}</textarea>
        </div>

        <div class="flex justify-end">
            <a href="{{ route('tasks.index') }}" class="px-4 py-2 bg-gray-200 rounded hover:bg-gray-300 mr-2">Annuler</a>
            <button type="submit" class="px-4 py-2 bg-hh-primary text-white rounded hover:bg-hh-primary-dark">Créer</button>
        </div>
    </form>
</div>
@endsection
