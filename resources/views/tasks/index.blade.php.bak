@extends('layouts.app')

@section('title', 'Tâches')

@section('content')
<div class="bg-white dark:bg-hh-gray-dark rounded-lg shadow p-6">
    <div class="flex justify-between items-center mb-4">
        <h2 class="text-xl font-semibold flex items-center space-x-2">
            <x-heroicon-o-clipboard-document-list class="w-6 h-6 text-hh-primary"/>
            <span>Liste des tâches</span>
        </h2>
        <a href="{{ route('tasks.create') }}" class="px-4 py-2 bg-hh-primary text-white rounded hover:bg-hh-primary-dark">
            <x-heroicon-o-plus class="w-5 h-5 inline"/> Nouvelle tâche
        </a>
    </div>

    <table class="min-w-full text-sm text-left">
        <thead class="bg-hh-gray-light dark:bg-hh-gray-darker uppercase text-gray-700 dark:text-gray-200">
            <tr>
                <th class="px-4 py-2">#</th>
                <th class="px-4 py-2">Titre</th>
                <th class="px-4 py-2">Projet</th>
                <th class="px-4 py-2">Assigné à</th>
                <th class="px-4 py-2">Statut</th>
                <th class="px-4 py-2">Échéance</th>
                <th class="px-4 py-2 text-right">Actions</th>
            </tr>
        </thead>
        <tbody>
            @forelse($tasks as $task)
                <tr class="border-b dark:border-hh-gray-darker hover:bg-gray-50 dark:hover:bg-hh-gray-darker/50">
                    <td class="px-4 py-2">{{ $loop->iteration }}</td>
                    <td class="px-4 py-2 font-medium">{{ $task->title }}</td>
                    <td class="px-4 py-2">{{ $task->project->name ?? '—' }}</td>
                    <td class="px-4 py-2">{{ $task->assignedTo->name ?? 'Non assigné' }}</td>
                    <td class="px-4 py-2">
                        <span class="px-2 py-1 text-xs rounded font-medium 
                            {{ $task->status === 'En cours' ? 'bg-yellow-100 text-yellow-700' :
                               ($task->status === 'Terminé' ? 'bg-green-100 text-green-700' : 
                               'bg-gray-100 text-gray-700') }}">
                            {{ $task->status }}
                        </span>
                    </td>
                    <td class="px-4 py-2">{{ $task->due_date?->format('d/m/Y') ?? '—' }}</td>
                    <td class="px-4 py-2 text-right space-x-2">
                        <a href="{{ route('tasks.show', $task) }}" class="text-blue-500 hover:text-blue-700">
                            <x-heroicon-o-eye class="w-5 h-5 inline"/>
                        </a>
                        <a href="{{ route('tasks.edit', $task) }}" class="text-yellow-500 hover:text-yellow-700">
                            <x-heroicon-o-pencil-square class="w-5 h-5 inline"/>
                        </a>
                        <form action="{{ route('tasks.destroy', $task) }}" method="POST" class="inline-block" onsubmit="return confirm('Supprimer cette tâche ?')">
                            @csrf @method('DELETE')
                            <button class="text-red-500 hover:text-red-700">
                                <x-heroicon-o-trash class="w-5 h-5 inline"/>
                            </button>
                        </form>
                    </td>
                </tr>
            @empty
                <tr>
                    <td colspan="7" class="text-center py-4 text-gray-500">Aucune tâche enregistrée.</td>
                </tr>
            @endforelse
        </tbody>
    </table>
</div>
@endsection
