@extends('layouts.app')

@section('content')
<div class="max-w-4xl mx-auto bg-black text-white shadow-lg rounded-2xl p-6 mt-10 border border-yellow-500">
    <h1 class="text-2xl font-bold text-yellow-500 mb-6">âœï¸ Modifier un EmployÃ©</h1>

    @if ($errors->any())
        <div class="bg-red-700 text-white p-3 rounded mb-4">
            <ul class="list-disc pl-5">
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif

    <form action="{{ route('employees.update', $employee->id) }}" method="POST">
        @csrf
        @method('PUT')
        <div class="grid grid-cols-2 gap-6">
            <div>
                <label class="block mb-1 font-semibold text-yellow-400">PrÃ©nom</label>
                <input type="text" name="first_name" value="{{ $employee->first_name }}" class="w-full p-2 rounded bg-gray-900 border border-yellow-600" required>
            </div>

            <div>
                <label class="block mb-1 font-semibold text-yellow-400">Nom</label>
                <input type="text" name="last_name" value="{{ $employee->last_name }}" class="w-full p-2 rounded bg-gray-900 border border-yellow-600" required>
            </div>

            <div>
                <label class="block mb-1 font-semibold text-yellow-400">Email</label>
                <input type="email" name="email" value="{{ $employee->email }}" class="w-full p-2 rounded bg-gray-900 border border-yellow-600">
            </div>

            <div>
                <label class="block mb-1 font-semibold text-yellow-400">Salaire de base</label>
                <input type="number" name="basic_salary" value="{{ $employee->basic_salary }}" class="w-full p-2 rounded bg-gray-900 border border-yellow-600">
            </div>

            <div>
                <label class="block mb-1 font-semibold text-yellow-400">DÃ©partement</label>
                <select name="department_id" class="w-full p-2 rounded bg-gray-900 border border-yellow-600">
                    <option value="">-- SÃ©lectionner --</option>
                    @foreach($departments as $dep)
                        <option value="{{ $dep->id }}" {{ $employee->department_id == $dep->id ? 'selected' : '' }}>
                            {{ $dep->name }}
                        </option>
                    @endforeach
                </select>
            </div>

            <div>
                <label class="block mb-1 font-semibold text-yellow-400">Filiale</label>
                <select name="filiale_id" class="w-full p-2 rounded bg-gray-900 border border-yellow-600">
                    <option value="">-- SÃ©lectionner --</option>
                    @foreach($filiales as $filiale)
                        <option value="{{ $filiale->id }}" {{ $employee->filiale_id == $filiale->id ? 'selected' : '' }}>
                            {{ $filiale->name }}
                        </option>
                    @endforeach
                </select>
            </div>

            <div>
                <label class="block mb-1 font-semibold text-yellow-400">Agence</label>
                <select name="agency_id" class="w-full p-2 rounded bg-gray-900 border border-yellow-600">
                    <option value="">-- SÃ©lectionner --</option>
                    @foreach($agences as $agence)
                        <option value="{{ $agence->id }}" {{ $employee->agency_id == $agence->id ? 'selected' : '' }}>
                            {{ $agence->name }}
                        </option>
                    @endforeach
                </select>
            </div>

            <div>
                <label class="block mb-1 font-semibold text-yellow-400">Utilisateur liÃ©</label>
                <select name="user_id" class="w-full p-2 rounded bg-gray-900 border border-yellow-600">
                    <option value="">-- Aucun --</option>
                    @foreach($users as $u)
                        <option value="{{ $u->id }}" {{ $employee->user_id == $u->id ? 'selected' : '' }}>
                            {{ $u->name }}
                        </option>
                    @endforeach
                </select>
            </div>
        </div>

        <div class="mt-6 flex justify-between">
            <a href="{{ route('employees.index') }}" class="text-yellow-400 hover:text-yellow-300">â¬… Retour</a>
            <button type="submit" class="bg-yellow-500 hover:bg-yellow-600 text-black font-semibold px-6 py-2 rounded">
                Mettre Ã  jour
            </button>
        </div>
    </form>
</div>
@endsection







