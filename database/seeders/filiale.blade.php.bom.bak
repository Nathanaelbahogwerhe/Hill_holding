@extends('layouts.app')

@section('content')
<div class="container">
    <h2 class="mb-4">ðŸ¢ Dashboard Filiale : {{ $filiale->name }}</h2>

    <div class="row">
        <div class="col-md-6">
            <h4>Agences</h4>
            <ul class="list-group">
                @foreach($filiale->agences as $agence)
                    <li class="list-group-item">{{ $agence->name }} ({{ $agence->code }})</li>
                @endforeach
            </ul>
        </div>

        <div class="col-md-6">
            <h4>Utilisateurs de la filiale</h4>
            <ul class="list-group">
                @foreach($users as $user)
                    <li class="list-group-item">{{ $user->name }} - {{ $user->email }}</li>
                @endforeach
            </ul>
        </div>
    </div>
</div>
@endsection







